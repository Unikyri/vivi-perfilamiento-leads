# Apply Progress: ProcesarMensaje Conversational Turn (PR 2 Edges)

## Status

- Change: `issue-19-procesar-mensaje`
- Mode: Standard (strict TDD disabled by project configuration)
- Delivery: feature-branch-chain, slice `edges`, hard authored budget 400 lines, no size exception
- Base: `a7451fd` (`origin/main` / PR 1 core)
- Snapshot authored lines: 328 (`311` additions + `17` deletions), within the 400-line cap
- Final measured PR2 code/test delta versus `origin/main`: `311` additions + `17` deletions = `328` authored lines; PASS, ≤400
- Completed tasks: 1.1, 1.2, 1.3, 2.1, 2.2, 3.1, 3.2
- Remaining tasks: none

## Implementation

- `internal/usecase/procesar_mensaje.go`: added pre-mutation two-pass recognized-field validation and integral JSON-number normalization; recognizes `reporte_credito` as boolean; preserves verified-base fields and downgrades provider verified provenance; recalculates capacity exactly once through `motor.CalcularCapacidad(perfil, afiliado, 0)`; isolates `AUDIO_ININTELIGIBLE` and `PAUSAR_CONTACTO` to response persistence; applies completion precedence, `PERFILANDO→CALIFICADO`, and post-durability `EvPerfilCompleto` publication; preserves LEAD→CAS→VIVI ordering and failure prefixes; removed unused `validarSalida`.
- `internal/usecase/siguiente_pregunta.go`: `SiguienteMejorPregunta` now unconditionally skips a present `VERIFICADO_BASE` field, including when its value is nil.
- `internal/usecase/procesar_mensaje_test.go`: added deterministic assertions for the nil verified-base helper case, all four captured motor numbers, and valid audio provider failure with exactly one audio call and zero writes; retained prior edge coverage.
- No ADK, HTTP, port, DTO, domain, Contract, migration, configuration, or runtime wiring changes.

## Work Unit Evidence

| Evidence | Result |
|---|---|
| Focused test | `go test ./internal/usecase -run 'TestProcesarMensajeEdges' -count=1` — PASS |
| Runtime harness | N/A — provider-free use case is intentionally unwired; injected repository/provider/bus fakes prove the public boundary and the threat matrix has no shell, routing, subprocess, VCS, or process integration |
| Rollback boundary | Revert only the PR2 remediation edits in `internal/usecase/procesar_mensaje.go`, `internal/usecase/siguiente_pregunta.go`, and `internal/usecase/procesar_mensaje_test.go`; preserve PR1 core and all ports/domain/contracts |

## Validation Evidence

- `go test ./internal/usecase -run 'TestProcesarMensajeEdges' -count=1` — PASS
- `go test ./internal/usecase -run 'TestProcesarMensajeAudioProviderErrorHasNoWrites|TestSiguientePreguntaCore' -count=1` — PASS
- `go test ./... -count=1` — PASS
- `go test -race ./... -count=1` — PASS
- `go build ./...` — PASS
- `go vet ./...` — PASS
- `go mod tidy && git diff --exit-code -- go.mod go.sum` — PASS; no module changes
- `gofmt -w internal/usecase/procesar_mensaje.go internal/usecase/siguiente_pregunta.go internal/usecase/procesar_mensaje_test.go` then `gofmt -l ...` — PASS; no output
- `git diff --check` — PASS
- `git diff --numstat origin/main -- internal/usecase/procesar_mensaje.go internal/usecase/siguiente_pregunta.go internal/usecase/procesar_mensaje_test.go` — `311 + 17 = 328`; PASS, ≤400

## Scope and Design Notes

- Implementation matches the approved design and keeps audio caller text as the only possible inbound transcript; raw bytes are not persisted and provider-returned transcription remains out of scope.
- Completion is suppressed for terminal consent/safety actions; `AUDIO_ININTELIGIBLE` and `PAUSAR_CONTACTO` persist only the Vivi response. Normal completion publishes only after inbound message, lead CAS, and response durability.
- All seven task boxes are checked in `tasks.md`; ready for verification.

## Remediation and Review Status

- Final-verification warnings addressed: W2 (verified-base nil skip), W3 (motor-number runtime assertions), W4 (audio provider error: one call/zero writes), and W6 (focused selector corrected).
- The existing receipt is superseded by this remediation and must be regenerated by the later verification/delivery process; no review was started or finalized here, and archive was not run.
